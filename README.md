# ml-clustering-classification-dr
A complete ML pipeline covering custom K-Means clustering with Manhattan distance, classification with 4 algorithms, and PCA vs LDA dimensionality reduction on two real-world datasets.
